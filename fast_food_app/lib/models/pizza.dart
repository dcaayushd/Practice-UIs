import 'package:flutter/material.dart';

class Pizza {
  String name;
  int price;
  Color color;
  String image;

  Pizza({
    required this.name,
    required this.price,
    required this.color,
    required this.image,
  });
}
